extends ../layouts/main.pug

block globals
  - title = "Process Documentor";

block content

  .container
    header
      h1
        = title
        br
    p Write out a process in a document allowing users to fill in details as nessesary for use later in the process or just to save them with the process.